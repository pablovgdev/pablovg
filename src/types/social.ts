import { ReactElement } from "react";

export type Social = {
  url: string;
  icon: ReactElement;
  name: string;
};

export type TechStack = {
  name: string;
  icon: ReactElement;
  url: string;
};
